# Build process

To build 3rd party libraries call python script form Window root folder

Example:
```sh
python3 "/thirdparty/build_dependencies.py
```

# Shaders

To build shaders go to `shaders` folder and run

```sh
python3 build_shaders.py
```

then copy shaders to `bin/{Platform}/Shaders